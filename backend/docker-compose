version: '3'
services:
  backend:
    build: .
    ports:
      - "8080:8080"

  db:
    image: postgres
    restart: always
    environment:
    - POSTGRES_USER=ninja
    - POSTGRES_PASSWORD=Welkom01!
    - POSTGRES_DB=ninja
    volumes:
      - ./postgres-data:/var/lib/postgresql/data

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
volumes:
  logvolume01: {}
